server:
  port: 10086
spring:
  application:
    name: eurekaserver   # eureka服务名称
eureka:
  client:
    service-url:   # eureka的地址信息，因为自己本身也是服务，需要进行注册，集群会有多个eureka，方便互相通信。多个,分割
      defaultZone: http://127.0.0.1:10086/eureka